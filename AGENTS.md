# Codex repository guidance

## このリポジトリについて

AIニケちゃん Thumbnail Studioは、背景、生成文字、部分フレーム、キャラクターを別レイヤーで合成するローカルWebアプリです。完全なワークフローはmacOS上のOpenAI Codexアプリ／CLIを前提とし、OpenAI Platform APIやAPIキーは使用しません。

作業を始める前に、`README.md` と `docs/SETUP.md` を読んでください。素材生成スケジューラへ変更を加える場合だけ、さらに `automations/ai-t7-10/workflow.md` を最初から最後まで読んでください。

## セットアップ依頼を受けたとき

ユーザーが「セットアップして」「どうすれば使える？」などと依頼した場合は、説明だけで終えず、現在の環境を調べ、安全に実行できるセットアップと検証まで進めてください。

1. `git status --short --branch` で既存変更を確認し、ユーザーの変更を上書きしない。
2. macOS、Node.js 22系、npm、CodexアプリまたはCLIの有無とバージョンを確認する。
3. `codex login status` を確認する。PATHにない場合はCodexアプリ同梱CLIも確認する。ChatGPTログイン以外の認証へ切り替えず、APIキーを要求しない。
4. 新しいクローンでは `npm ci` を実行する。依存関係が既に揃っている場合は不要な再インストールをしない。
5. 素材ライブラリを決める。
   - ユーザー所有の書き込み可能なディレクトリを選び、Git管理外の `.env` に `THUMBNAIL_LIBRARY_ROOT` として設定する。
   - 既存ライブラリの場所を推測したり、作者の保存先を案内したりしない。新規利用者には自分のホームディレクトリ内など、本人の環境に属する候補を提示する。
6. `npm run dev` を起動し、`http://127.0.0.1:4178/api/health` とブラウザ画面を確認する。
7. 最後に、使用した保存先、起動URL、Codex画像修正の利用可否、実行した検証、残る手動作業を短く報告する。

選択が結果を大きく変える場合だけ質問してください。安全な既定値で進められる作業は進め、勝手にLaunchAgentや素材生成automationを登録しないでください。

## プロダクトの不変条件

- キャンバス上の背景は常に1枚、キャラクターは常に1人。新規選択時は既存レイヤーを置換する。
- キャラクターは頭部アンカーを使って顔寄せ配置する。
- テーマは背景1点、生成文字最大2点、角飾りまたは上下帯の部分フレーム1点を基本とする。
- 文字素材はすべて画像生成する。OSフォント、Webフォント、Canvas文字、SVG文字で代用しない。
- `split-character` の「朝」「活」は別々に生成する。結合済み画像を切断しない。
- `diagonal-pair` は同じ透過PNG内で「朝」を左上、「活」を右下へ組んだ一体ロゴとして生成する。
- 任意小物、四辺フレーム、小窓風背景、背景内の可読文字・疑似文字を再導入しない。
- 完成構図は「背景シンプル、キャラでっかく、文字でっかく、小物少々」を優先する。
- 「Codexで修正」はChatGPTサブスクリプションで認証されたローカル `codex exec` とbuilt-in image generationだけを使う。Platform APIへフォールバックしない。

## 実装案内

- `src/App.tsx`: エディタUI、テーマ適用、Codex修正画面
- `src/lib.ts`: 配置、置換、保存データ正規化、構成チェック
- `src/types.ts`: プロジェクト、レイヤー、テーマ、Codexジョブの型
- `server/index.ts`: ローカルHTTP API
- `server/storage.ts`: 素材ライブラリとプロジェクトの保存
- `server/codex-edits.ts`: Codex画像修正ジョブ
- `automations/ai-t7-10/workflow.md`: 素材生成契約の正本

## 変更と検証

- 変更は依頼範囲に限定し、無関係なdirty/untrackedファイルを変更、削除、コミットしない。
- `THUMBNAIL_LIBRARY_ROOT` は素材ライブラリの正本。明示された素材操作以外では変更しない。
- `.env`、利用者の絶対パス、CodexプロジェクトIDなどのローカル設定をコミットしない。
- `references/vtuber-thumbnails/` はスケジューラの作業成果なので、通常のアプリ変更では触らない。
- コード変更後は `npm test`、`npm run build`、`git diff --check` を実行する。
- UI変更はDOMやコードだけで完了扱いにせず、実ブラウザで関連するクリック、ホイール、置換、スクロール、保存・再読込を確認する。
- コミットやpushはユーザーが依頼した場合に行い、意図したファイルだけをstageする。
