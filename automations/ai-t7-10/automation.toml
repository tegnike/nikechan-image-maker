version = 1
id = "thumbnail-materials"
kind = "cron"
name = "Thumbnail Studio 素材生成"
prompt = "作業ディレクトリにある automations/ai-t7-10/workflow.md を最初から最後まで読み、書かれた手順に従って生成スロットを1つだけ実行してください。workflow.mdをこの自動処理の正本として扱い、要約や旧手順で代用しないでください。外部Webサイトから取得したサムネイル画像は構成分析の一時確認にだけ使い、built-in image_genを含むいかなる画像生成にも画像入力として絶対に渡さないでください。background生成は保存済みのgeneration_briefとテーマ仕様だけをテキストで渡し、referenced_image_pathsとnum_last_images_to_includeを省略してください。この条件を満たせないrunは生成せず失敗終了してください。"
status = "ACTIVE"
rrule = "RRULE:FREQ=MINUTELY;INTERVAL=10"
model = "gpt-5.5"
reasoning_effort = "medium"
notification_policy = "failed_runs_only"
execution_environment = "local"
target = { type = "project", project_id = "REPLACE_WITH_YOUR_CODEX_PROJECT_ID" }
cwds = ["/absolute/path/to/nikechan-image-maker"]
created_at = 1785155686079
updated_at = 1785322910921
